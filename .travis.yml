language: python # => 1
    python: # => 2
      - "2.6"
      - "2.7"
      - "3.7"
    env: # => 4
      -DJANGO=1.8 DB=mysql
    install: # => 5
      - pip install -r requirements.txt
    script: # => 7
      - python manage.py test
